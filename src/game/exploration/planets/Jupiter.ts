import { PlanetProfile } from '../types';

export const JupiterProfile: PlanetProfile = {
    introMessages: ['Entorno gaseoso extremo, orbita controlada.'],
    dangerOverrides: { gravityG: 1.2, radiation: 60 }
};
